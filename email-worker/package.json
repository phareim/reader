{
  "name": "reader-email",
  "private": true,
  "version": "1.0.0",
  "description": "reader@phareim.no → Reader Found feed (see ../docs/email-ingest.md)",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "postal-mime": "^2.4.3"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  },
  "overrides": {
    "undici": "^7.29.0"
  }
}
