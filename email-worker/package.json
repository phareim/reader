{
  "name": "reader-email",
  "private": true,
  "version": "1.0.0",
  "description": "reader@phareim.no → Reader Found feed (see ../docs/email-ingest.md)",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "postal-mime": "^2.4.3"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
