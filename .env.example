# Database
# For local development: file:./data/reader.db
# For Vercel: Use Vercel Postgres, Neon, or PlanetScale connection string
DATABASE_URL="file:./data/reader.db"

# Feed Fetching
FETCH_TIMEOUT=30000
MAX_ARTICLES_PER_FEED=500

# Auth Configuration (CRITICAL - Generate with: openssl rand -base64 32)
AUTH_SECRET="your-super-secret-auth-secret-change-this-in-production"

# Auth Origin - Update for production
# Local: http://localhost:3000
# Production: https://your-app.vercel.app
AUTH_ORIGIN="http://localhost:3000"

# Google OAuth
# Get credentials from: https://console.cloud.google.com/apis/credentials
# Authorized redirect URIs must include: https://your-app.vercel.app/api/auth/callback/google
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
